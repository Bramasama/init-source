---
const items = [
  "2-5 candidates aligned to the scorecard",
  "Availability / notice period",
  "Compensation expectations",
  "Must-haves + gaps / risks",
  "Interview notes (summary)",
  "Next steps + fast feedback loops"
];
---

<section class="section bg-white transition-colors dark:bg-slate-950">
  <div class="container maxw">
    <h2 class="text-4xl font-extrabold text-slate-900 transition-colors md:text-5xl dark:text-slate-100">
      What you get <span class="text-[#007BFF] dark:text-accent">(every shortlist)</span>
    </h2>
    <p class="mt-3 max-w-2xl text-lg text-slate-500 dark:text-slate-300">
      Clear, structured insight so you can decide quickly and confidently.
    </p>
    <div class="mt-10 grid gap-6 md:grid-cols-2">
      {items.map((item) => (
        <div class="flex items-start gap-4 rounded-2xl border border-gray-200 bg-white p-6 shadow-sm transition dark:border-slate-700 dark:bg-slate-900/70">
          <span class="mt-1 inline-grid size-8 place-items-center rounded-full bg-blue-50 text-[#007BFF] dark:bg-blue-500/20">
            ✓
          </span>
          <p class="text-lg text-slate-700 dark:text-slate-200">{item}</p>
        </div>
      ))}
    </div>
  </div>
</section>
